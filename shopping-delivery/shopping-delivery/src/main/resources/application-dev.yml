spring:
    datasource:
        url: jdbc:h2:mem:deliveryDB
        username: root
        password:
    h2:
        console:
            enabled: true
            path: /h2
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true